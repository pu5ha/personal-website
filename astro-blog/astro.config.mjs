import { defineConfig } from 'astro/config';

// NOTE: Update `site` and `base` before deploying to GitHub Pages.
// Example:
// site: 'https://<YOUR_USERNAME>.github.io/<REPO_NAME>',
// base: '/<REPO_NAME>',

export default defineConfig({
  site: 'https://<YOUR_USERNAME>.github.io/<REPO_NAME>',
  base: '/<REPO_NAME>'
});


